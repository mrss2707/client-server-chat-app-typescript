#!/usr/bin/env node
"use strict";

// Function to start project
var server = require("../build/ChatServer");
var mainConfig = require("../configs/mainConfig");
var chatServer = server.ChatServer.Init();
chatServer.LoadConfig(mainConfig);
